import React from 'react';
import { useTranslation } from 'react-i18next';
// import './home.less';

const Home: React.FC = () => {
  const { t } = useTranslation();

  return (
    <div className="home">
      test
    </div>
  );
};

export default Home;
